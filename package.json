{
  "name": "local-agent-coder",
  "displayName": "Local Agent Coder",
  "description": "Agent + Plan coding assistant for local LM Studio models.",
  "version": "0.0.102",
  "publisher": "local",
  "engines": {
    "vscode": "^1.87.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:localAgent.chatView",
    "onCommand:localAgent.runTask",
    "onCommand:localAgent.planTask",
    "onCommand:localAgent.reloadModels"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "localAgent",
          "title": "Local Agent",
          "icon": "media/robot.svg"
        }
      ]
    },
    "views": {
      "localAgent": [
        {
          "type": "webview",
          "id": "localAgent.chatView",
          "name": "Agent + Plan"
        }
      ]
    },
    "commands": [
      {
        "command": "localAgent.runTask",
        "title": "Local Agent: Run Task (Plan + Execute)"
      },
      {
        "command": "localAgent.planTask",
        "title": "Local Agent: Create Plan Only"
      },
      {
        "command": "localAgent.reloadModels",
        "title": "Local Agent: Reload Models"
      }
    ],
    "configuration": {
      "title": "Local Agent",
      "properties": {
        "localAgent.lmStudio.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:1234/v1",
          "description": "LM Studio OpenAI-compatible base URL."
        },
        "localAgent.lmStudio.apiKey": {
          "type": "string",
          "default": "",
          "description": "API key for LM Studio endpoint. Leave empty only when LM Studio auth is disabled."
        },
        "localAgent.lmStudio.model": {
          "type": "string",
          "default": "qwen2.5-coder-7b-instruct",
          "description": "Model name loaded in LM Studio."
        },
        "localAgent.lmStudio.preset": {
          "type": "string",
          "default": "",
          "description": "Optional LM Studio preset name to attach to API requests."
        },
        "localAgent.lmStudio.integrations": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Optional LM Studio MCP integration IDs for native chat endpoint. Example: [\"mcp/duckduckgo\",\"mcp/web-search\",\"mcp/context7\"]. Empty = auto-discover from ~/.lmstudio/mcp.json."
        },
        "localAgent.provider.preset": {
          "type": "string",
          "enum": [
            "lmstudio",
            "custom"
          ],
          "default": "lmstudio",
          "description": "Backend provider preset. 'custom' works with any OpenAI-compatible API."
        },
        "localAgent.provider.baseUrl": {
          "type": "string",
          "default": "",
          "description": "Override base URL for provider preset."
        },
        "localAgent.provider.apiKey": {
          "type": "string",
          "default": "",
          "description": "Optional provider API key. If set, extension will prefer this key for both provider presets."
        },
        "localAgent.provider.model": {
          "type": "string",
          "default": "",
          "description": "Override model name for provider preset. Leave empty to reuse localAgent.lmStudio.model."
        },
        "localAgent.provider.apiMode": {
          "type": "string",
          "enum": [
            "chat_completions",
            "responses",
            "lm_rest_chat"
          ],
          "default": "lm_rest_chat",
          "description": "Endpoint mode. chat_completions/responses are OpenAI-compatible. lm_rest_chat uses LM Studio native /api/v1/chat (required for local mcp.json integrations)."
        },
        "localAgent.provider.modelsPath": {
          "type": "string",
          "default": "/models",
          "description": "Path for model list endpoint on OpenAI-compatible APIs."
        },
        "localAgent.provider.chatPath": {
          "type": "string",
          "default": "/chat/completions",
          "description": "Path for chat completion endpoint on OpenAI-compatible APIs."
        },
        "localAgent.provider.extraHeaders": {
          "type": "string",
          "default": "{}",
          "description": "Extra HTTP headers as JSON object string. Example: {\"X-Key\":\"value\"}"
        },
        "localAgent.provider.extraBody": {
          "type": "string",
          "default": "{}",
          "description": "Extra chat body JSON merged into each request (except protected keys like model/messages/stream). Example: {\"preset\":\"my-preset\"}"
        },
        "localAgent.provider.presetName": {
          "type": "string",
          "default": "",
          "description": "Optional provider preset name. If set and extraBody has no preset, request body will include {\"preset\":\"...\"}."
        },
        "localAgent.inline.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable inline code completion in editor (Copilot-like ghost text)."
        },
        "localAgent.inline.model": {
          "type": "string",
          "default": "",
          "description": "Optional model override for inline completion. Empty uses localAgent.lmStudio.model."
        },
        "localAgent.inline.debounceMs": {
          "type": "number",
          "default": 180,
          "minimum": 0,
          "maximum": 2000,
          "description": "Debounce delay before requesting inline completion."
        },
        "localAgent.inline.maxTokens": {
          "type": "number",
          "default": 160,
          "minimum": 32,
          "maximum": 400,
          "description": "Maximum completion tokens for inline suggestions."
        },
        "localAgent.inline.maxContextChars": {
          "type": "number",
          "default": 6000,
          "minimum": 1200,
          "maximum": 20000,
          "description": "Maximum context characters sent to model for inline completion."
        },
        "localAgent.inline.maxSuggestionChars": {
          "type": "number",
          "default": 420,
          "minimum": 80,
          "maximum": 4000,
          "description": "Maximum characters returned for one inline suggestion."
        },
        "localAgent.inline.maxCandidates": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 6,
          "description": "Number of inline completion candidates returned for cycling suggestions."
        },
        "localAgent.inline.minPrefixChars": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "maximum": 12,
          "description": "Minimum non-space chars before cursor required to trigger inline suggestion."
        },
        "localAgent.inline.verboseLog": {
          "type": "boolean",
          "default": false,
          "description": "Write inline completion debug logs to Local Agent Coder output."
        },
        "localAgent.inline.thinking.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable thinking mode for inline completion requests."
        },
        "localAgent.inline.thinking.effort": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "medium",
          "description": "Reasoning effort for inline completion when inline thinking mode is enabled."
        },
        "localAgent.thinking.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable thinking mode for planning/execution requests."
        },
        "localAgent.thinking.effort": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "medium",
          "description": "Reasoning effort for planning/execution when thinking mode is enabled."
        },
        "localAgent.maxTurnsPerStep": {
          "type": "number",
          "default": 14,
          "minimum": 3,
          "maximum": 30,
          "description": "Maximum agent turns per plan step."
        },
        "localAgent.guardMode": {
          "type": "string",
          "enum": [
            "strict",
            "balanced",
            "relaxed"
          ],
          "default": "relaxed",
          "description": "How strict runtime guards are. relaxed = fewer blocks and more exploration freedom."
        },
        "localAgent.systemPromptMode": {
          "type": "string",
          "enum": [
            "strict",
            "provider_first"
          ],
          "default": "strict",
          "description": "strict = extension prompt rules dominate. provider_first = lighter built-in prompts so provider preset/system prompt can lead more."
        },
        "localAgent.strictResponsibilityMode": {
          "type": "boolean",
          "default": false,
          "description": "Run an internal responsibility review before execute actions (write/run/complete/final) to block off-scope behavior."
        },
        "localAgent.executorMaxTokens": {
          "type": "number",
          "default": 7000,
          "minimum": 600,
          "maximum": 8000,
          "description": "Max completion tokens for each executor turn. Increase if write_file JSON gets truncated."
        },
        "localAgent.maxAskUser": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "maximum": 3,
          "description": "Maximum times the agent can ask the user in one task."
        },
        "localAgent.minInvestigationsBeforeExecute": {
          "type": "number",
          "default": 1,
          "minimum": 1,
          "maximum": 6,
          "description": "Minimum list/search/read actions before write/run/complete to reduce blind edits."
        },
        "localAgent.strategyCandidates": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "maximum": 5,
          "description": "Number of candidate implementation approaches to evaluate before choosing the plan strategy."
        },
        "localAgent.commandTimeoutMs": {
          "type": "number",
          "default": 120000,
          "minimum": 5000,
          "maximum": 600000,
          "description": "Timeout for shell command execution."
        },
        "localAgent.autoApplyWrites": {
          "type": "boolean",
          "default": true,
          "description": "Allow agent to write files directly."
        },
        "localAgent.systemPromptExtra": {
          "type": "string",
          "default": "",
          "description": "Additional system prompt content appended to planner and agent prompts."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/vscode": "^1.87.0",
    "typescript": "^6.0.2"
  }
}
